"""Third-party dependencies package."""

package(default_visibility = ["//visibility:public"])

# BUILD files for external repositories are referenced via build_file attribute
# in new_local_repository rules (see MODULE.bazel)
exports_files([
    "arrow.BUILD",
    "lean_test.BUILD",
    "lean_toolchain.BUILD",
    "libtorch.BUILD",
    "openmp.BUILD",
])
